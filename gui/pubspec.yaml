name: bocker_gui
description: "Material 3 desktop interface for Bocker on Ubuntu."
publish_to: 'none'
version: 2.0.1+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
