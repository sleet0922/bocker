name: bocker_gui
description: "Material 3 desktop interface for Bocker on Ubuntu."
publish_to: 'none'
version: 3.3.6+31

environment:
  sdk: ^3.12.2

dependencies:
  file_selector: ^1.1.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
