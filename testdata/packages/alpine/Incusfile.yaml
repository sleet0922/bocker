version: 1
mirror: china
name: bocker-packages-alpine-e2e
network: nat
stages:
  - from: alpine/3.21
    steps:
      - pkg: [ca-certificates, curl]
      - shell: |
          set -eu
          grep -F 'mirrors.tuna.tsinghua.edu.cn/alpine' /etc/apk/repositories
          curl --version > /curl-version.txt
    runtime: {autostart: false}
